.b-admin-content
  .b-admin-content-title Waiting for your approval:
  .list
    .list-header
      .nav
        = link_to '#', :class => 'movies selected' do
          %span
          Movies
        = link_to '#', :class => 'articles' do
          %span
          Articles
        = link_to '#', :class => 'presentations' do
          %span
          Presentations
      .list-title
        um, nisi vel venenatis faucibus, augue libero commodo tellus, nec 
        pharetra risus erat sit amet felis. Nullam at ipsum ac ante
      .clear
      .list-content
        - if @items.present?
          - @items.each do |item|
            = link_to moderator_item_path(item), :class => "item" do
              .media= image_tag '/assets/tmp/p2.png'
              .text= item.description.truncate(120, :separator => " ")
          .clear
          = paginate @items, :remote => true

        - else
          .no-items No items

        .clear