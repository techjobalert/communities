- if notice.present?
  .notice= notice
= form_for([commentable, commentable.comment_threads.build], :url => url, :remote => :true) do |f|
  %p= f.text_area :body, :rows=>"5"
  %p= f.submit "Комментировать"