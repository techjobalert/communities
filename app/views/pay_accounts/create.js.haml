$("#ajax-main-container").html("#{escape_javascript(render :partial => 'account/payments_info')}");
- if @notice.present?
  showHideNotice("#{@notice[:type]}", "#{@notice[:message]}");

-unless @notice.present?
  $("#account-form").dialog( "close" );
$(".payments-info").addClass("selected");