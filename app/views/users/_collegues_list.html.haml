.b-colleagues-container
  - if users.present?
    - users.each do |user|
      .b-colleagues-item
        = link_to image_tag(user.avatar_url(:thumb_60), :class => 'popup-user-info', :id => "popup-user-#{user.id}"), user_path(user), :class => "pjax"
        = link_to text_with_br(user.full_name), user_path(user), :class => "pjax title"
  - else
    .no-items No items

  .clear
