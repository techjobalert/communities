var fbutton = $(".follow-user-#{@following_user.id}");
if (fbutton){
fbutton.removeClass("unfollow").addClass("follow");
fbutton.data("method", "post").attr("href", "#{user_follow_path(@following_user)}");
}