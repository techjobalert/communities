- if @type == "public" || @user != current_user
  = render :partial => 'show', :locals => {:user => @user, :collaborators => @collaborators}
- else
  = render :partial => 'profile', :locals => {:user => @user, :collaborators => @collaborators}