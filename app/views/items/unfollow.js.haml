var fbutton = $(".follow-item-#{@following_item.id}");
if (fbutton){
fbutton.text("Follow item").append("<span/>");
fbutton.data("method", "post").attr("href", "#{item_follow_path(@following_item)}");
}