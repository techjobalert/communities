var fbutton = $(".light-button.follow");
if (fbutton){
	fbutton.text("Follow Item");
	var link = fbutton.parent("a");
	link.data("method", "post").attr("href", '<%= item_follow_path(@following_item) %>');
}